fix this website according to the next instructions:

- make the pages that are listed on top (o nas, storitve, ...) into separate pages that you can jump between using those buttons instead of just scroll points

- make it so if i click off the top bar when it is extended in teh thin screen mode it collapses

- Make the cards in the "nase storitve" tab expand on click, add more info when expanded, add some rates that would make sense (for the clients to see)

- Make hte "kako poteka" cards extendible in the same way for more informatioon

- Make the certificates bigger on hover and jump to the link for more info about them on click (if not found jump to some random wiki page)

- Populate the references with some pictures and real lookign data, make that into a separate page with the others